# 项目开发核心指令 (SKILL)

1. **输入记录**
   - 在项目根目录创建 `提示词.txt`。
   - 将所有用户输入（包括后续输入）自动追加保存到该文件中。
   - 确保内容不被覆盖，防止乱码。

2. **文档管理**
   - 创建文件夹 `文档`。
   - AI生成的所有文档（需求、设计、计划等）必须存放在此文件夹下。

3. **沟通记录**
   - 创建文件夹 `AI沟通记录`。
   - 保存聊天记录到此文件夹。
   - 如果记录过长，自动进行文件分割。

4. **任务与测试**
   - 创建文件夹 `任务和测试过程`。
   - 存放 `task.md`、测试报告及相关过程文档。

5. **初始化**
   - 运行指令：`npx uipro-cli init --ai antigravity`。
   - 如遇问题参考：https://github.com/nextlevelbuilder/ui-ux-pro-max-skill

6. **核心原则**
   - **简洁至上 (KISS)**：避免过度设计。
   - **深度分析**：第一性原理思考。
   - **事实为本**：有错必纠。
   - **最小改动**：只改动必要代码，不进行无关格式化。

7. **工作流**
   - **渐进式开发**：构思 -> 审核 -> 任务分解 -> 执行。
   - **语言**：中文。
